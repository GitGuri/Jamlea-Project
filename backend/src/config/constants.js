// Single global reorder point used for low-stock alerts. Not admin-configurable
// via the UI yet -- change here if the number needs to move.
const LOW_STOCK_THRESHOLD = 10;

module.exports = { LOW_STOCK_THRESHOLD };
